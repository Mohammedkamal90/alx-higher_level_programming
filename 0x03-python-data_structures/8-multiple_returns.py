#!/usr/bin/python3
# 8-multiple_returns.py


def multiple_returns(sentence):
    """return a length of string and first character"""
    if sentenc == "":
        return (0, None)
    return (len(sentenc), sentenc[0])
